/**
 * Stepper — placeholder test suite (chunk: spa-consultation-input-overhaul).
 *
 * TODO(tester): flesh these out with @testing-library/react.
 */

describe('Stepper', () => {
  it.todo('renders minus, value, and plus controls with role=group');
  it.todo('emits onChange(stringified clamped value) when plus is clicked');
  it.todo('emits onChange(stringified clamped value) when minus is clicked');
  it.todo('disables minus at min and plus at max');
  it.todo('uses defaultValue when value prop is empty string');
  it.todo('supports custom step prop (e.g. step=2)');
  it.todo('renders suffix string after the value display when provided');
  it.todo('value display is wrapped in <output aria-live="polite"> for SR announcements');
});
