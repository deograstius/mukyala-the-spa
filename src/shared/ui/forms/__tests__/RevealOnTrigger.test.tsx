/**
 * RevealOnTrigger — placeholder test suite (chunk: spa-consultation-input-overhaul).
 *
 * TODO(tester): flesh these out with @testing-library/react.
 */

describe('RevealOnTrigger', () => {
  it.todo('renders children when show=true');
  it.todo('does not render children when show=false (default keepMounted=false)');
  it.todo('keeps children mounted with hidden attribute when keepMounted=true and show=false');
  it.todo('respects prefers-reduced-motion (no transition applied)');
  it.todo('applies slide+fade transition when prefers-reduced-motion: no-preference');
  it.todo('clears form value via parent onChange when transitioning out (via applyRevealClears)');
});
