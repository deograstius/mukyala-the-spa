// Shared inline form styles for the admin surfaces — same values the /retail
// POC used, kept in one place now that several pages share them.
export const inputStyle: React.CSSProperties = {
  width: '100%',
  padding: '12px 14px',
  borderRadius: 8,
  border: '1px solid #d5cec4',
  fontSize: 16,
};

export const labelStyle: React.CSSProperties = {
  display: 'block',
  marginBottom: 6,
  fontWeight: 600,
};
